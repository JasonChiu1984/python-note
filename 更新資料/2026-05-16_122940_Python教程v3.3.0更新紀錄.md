# Python 教程 v3.3.0 更新紀錄

- 更新日期：2026-05-16
- 完整更新時間：2026-05-16 12:29:40 CST
- 對應審查報告：`審查報告/2026-05-16_122940_Python教程資深工程審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-05-16_122940_Python教程v3.3.0內容需要更新的部分.md`
- 更新前版本：v3.2.0
- 更新後版本：v3.3.0

## 1. 本輪更新摘要

本輪依審查報告補上「資料演進治理」獨立路線，讓教程能更完整涵蓋真實 Python 專案中的 schema version、compat read/write、expand/contract、backfill checkpoint、drift preflight 與 rollback evidence。

## 2. 修改內容

| 類型 | 路徑 | 說明 |
|---|---|---|
| 新增教材 | `python_data_evolution_governance_tutorial.html` | 32 張互動卡，涵蓋 schema、backfill、rollback、evidence |
| 新增範例 | `範例程式碼/data_evolution_governance/` | 標準庫 data evolution governance sample |
| 更新入口 | `index.html` | 新增第 23 條教材卡片與進度 key |
| 更新版本 | `VERSION` | 升版為 v3.3.0 |
| 更新紀錄 | `CHANGELOG.md` | 新增 v3.3.0 條目 |

## 3. 驗證結果

本輪已完成以下驗證：

- `git diff --check`：通過
- `python3 -m py_compile 範例程式碼/data_evolution_governance/src/data_evolution_governance/*.py 範例程式碼/data_evolution_governance/scripts/run_data_evolution_smoke.py`：通過
- `PYTHONPATH=範例程式碼/data_evolution_governance/src python3 -m unittest discover -s 範例程式碼/data_evolution_governance/tests -v`：通過，共 6 項測試
- `PYTHONPATH=範例程式碼/data_evolution_governance/src python3 範例程式碼/data_evolution_governance/scripts/run_data_evolution_smoke.py`：通過，輸出 `data evolution smoke passed: compat + checkpoint + rollback + evidence`
- `node -e "...new Function(script)..."`：通過，HTML 教材 script parse 正常

## 4. 風險與注意事項

- 範例以標準庫模擬 schema 演進、backfill 與 rollback，目的是教學與本機驗收，不是完整 ORM migration framework。
- 正式專案若接 PostgreSQL、MySQL、Alembic、Kafka schema registry 或實際 OT telemetry store，仍需依平台補 transaction、locking 與 release orchestration。
- 工業命令與設備設定欄位若涉及現場控制，正式環境必須先驗證 fail-safe 與回退策略，不可直接套用 telemetry 欄位升版流程。

## 5. 推送資訊

本輪將以 `v3.3.0` 作為版本標記，並僅提交本次自動化新增或直接更新的檔案；工作樹內其他既有未提交教材修改不納入本次發布。
