# Python 教程 v4.2.14 更新紀錄

- 更新日期：2026-06-05
- 更新時間：2026-06-05 06:46:04 CST
- 更新版本：`v4.2.14`

## 本輪更新內容

| 類別 | 更新內容 |
|---|---|
| 專題頁版本同步 | 將 `python_cli_automation_engineering_tutorial.html` 的 title 與 hero status 版本由 `v4.2.12` 更新為 `v4.2.14` |
| 專題頁版本同步 | 將 `python_local_knowledge_base_mcp_tutorial.html` 的 title 與 hero status 版本由 `v4.2.12` 更新為 `v4.2.14` |
| 專題頁版本同步 | 將 `python_project_delivery_blueprint_tutorial.html` 的 title、eyebrow 與 footer 版本由 `v4.2.12` 更新為 `v4.2.14` |
| 發版資訊同步 | 將 `VERSION` 由 `v4.2.13` 更新為 `v4.2.14` |
| 發版資訊同步 | 在 `CHANGELOG.md` 新增 `v4.2.14 - 2026-06-05` 條目 |
| 首頁同步 | 更新 `index.html` 的版本狀態摘要，標示本輪完成三個專題教材頁版本追溯再同步並與 dated release artifacts 對齊 |
| 交付物補齊 | 新增本輪 `審查報告`、`內容需要更新的部分`、`更新資料` 三份日期化文件 |

## 驗證結果

- `python3 scripts/validate_tutorial_catalog.py`
  - 結果：通過，`index.html` 的教程連結、檔案與 `showProgress(...)` 對齊。
- `python3 -m py_compile scripts/export_conversation.py mcp_server/knowledge_base_server.py`
  - 結果：通過，未發現語法錯誤。
- `python3 -m unittest tests/test_knowledge_base_tooling.py -v`
  - 結果：通過，2 個測試全綠。
- `python3 scripts/run_knowledge_base_mcp_smoke.py`
  - 結果：通過，`export + search + read + protocol` smoke 正常。
- `git diff --check`
  - 結果：通過，未發現 whitespace 或 patch 格式問題。
- `rg -n "v4\\.2\\.12" python_cli_automation_engineering_tutorial.html python_local_knowledge_base_mcp_tutorial.html python_project_delivery_blueprint_tutorial.html`
  - 結果：無殘留輸出，表示三個本輪目標頁不再殘留舊版 `v4.2.12` 標記。

## 發版紀錄

- `git commit -m "Release Python tutorial v4.2.14"`
  - 結果：已建立 release commit `2804707a470eab047abaa9997e250eee6394c827`。
- `git commit -m "Finalize Python tutorial v4.2.14 release record"`
  - 結果：已建立最終 release record commit `342e9db7211dca0391aeeeb8ce705ea27e8ae665`。
- `git commit -m "Document Python tutorial v4.2.14 push result"`
  - 結果：已建立 push result metadata commit `40dfb817942a2a12cd519b04c66d726feb9e4d9f`。
- `git commit -m "Align Python tutorial v4.2.14 release refs"`
  - 結果：已建立 release refs 對齊 commit，將本輪最終 published state 收斂到同一個 release head。
- `git tag -a v4.2.14 -m "Python tutorial v4.2.14"`
  - 結果：已建立並重新對齊 annotated tag `v4.2.14`，最終以本檔完成後的 release head 為準。
- `git push origin main --follow-tags`
  - 結果：成功，遠端 `main` 先由 `3517ba4` 推進到 release record / push metadata commit，之後再推進到 release refs 對齊 commit；首次 `--follow-tags` 僅補推舊 tag `v4.2.8`，後續已補執行 `git push origin refs/tags/v4.2.14` 並重新對齊 tag。
