# Python 教程 v3.8.0 更新紀錄

- 更新日期：2026-05-17
- 完整更新時間：2026-05-17 13:39:49 CST
- 對應審查報告：`審查報告/2026-05-17_133949_Python教程資深工程審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-05-17_133949_Python教程v3.8.0內容需要更新的部分.md`
- 更新前版本：v3.7.0
- 更新後版本：v3.8.0

## 1. 本輪更新摘要

本輪新增 `Batch Scheduler Engineering` 獨立教材與零外部套件範例專案，補齊 Python 教程在長期批次與排程治理層對 lease、checkpoint、retry budget、dead-letter、backfill 與 rerun evidence 的缺口。

## 2. 修改內容

| 類型 | 路徑 | 說明 |
|---|---|---|
| 新增教材 | `python_batch_scheduler_engineering_tutorial.html` | 新增批次與排程工程專題頁 |
| 新增範例 | `範例程式碼/batch_scheduler_engineering/` | lease / checkpoint / retry / dead-letter / backfill / smoke / unittest |
| 更新入口 | `index.html` | 新增專題卡片、完成度與版本敘述 |
| 更新版本 | `VERSION` | 升版為 `v3.8.0` |
| 更新紀錄 | `CHANGELOG.md` | 新增 `v3.8.0` 條目 |
| 審查交付物 | `審查報告` / `內容需要更新的部分` / `更新資料` | 補齊本輪自動化產物 |

## 3. 驗證結果

本輪已完成以下驗證：

- `git diff --check`：通過
- `python3 -m py_compile 範例程式碼/batch_scheduler_engineering/src/batch_scheduler_engineering/*.py 範例程式碼/batch_scheduler_engineering/scripts/run_batch_scheduler_smoke.py`：通過
- `PYTHONPATH=範例程式碼/batch_scheduler_engineering/src python3 -m unittest discover -s 範例程式碼/batch_scheduler_engineering/tests -v`：通過（5 tests）
- `PYTHONPATH=範例程式碼/batch_scheduler_engineering/src python3 範例程式碼/batch_scheduler_engineering/scripts/run_batch_scheduler_smoke.py`：通過
- `python_batch_scheduler_engineering_tutorial.html` / `index.html` script parse：通過

## 4. 風險與注意事項

- 工作樹內原本就存在多個未提交 HTML 調整與未追蹤資料夾，本輪不回退，也不整批納入版本發布。
- 本輪新增的是批次與排程治理教材與範例，不會直接連接真實 cron、Celery 或外部排程系統。
- commit 與 push 時應只納入本輪明確管理的教材、範例、版本與交付檔案。

## 5. 推送資訊

- 內容提交：`842e9fd` (`Add Python batch scheduler tutorial v3.8.0`)
- 最終發布提交：`47c6d81` (`Finalize Python v3.8.0 publication record`)
- 版本標籤：`v3.8.0`
- `git push origin main`：成功（`e1419e3..842e9fd`，之後 `842e9fd..47c6d81`）
- `git push origin v3.8.0`：首次成功建立 tag，之後強制更新至最終發布提交（`ff25245...f0c45c7`）
- 目前本地 `HEAD`、`origin/main` 與 `v3.8.0^{}` 已對齊到 `47c6d81`
