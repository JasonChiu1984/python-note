# Python 教程 v1.0.0 更新紀錄

- 產出時間：2026-05-13 02:02:40 CST
- 對應審查報告：`審查報告/2026-05-13/2026-05-13_020240_Python教程資深工程審查報告.md`
- 版本標註：`v1.0.0`
- 更新目的：依資深工程審查補上 Python 3.14 / 3.15 版本現代化與 runtime readiness 路線，讓教程能涵蓋目前 Python 穩定版與預覽版對 production 專案的影響。

## 一、審查後修正摘要

| 類別 | 修改內容 | 檔案 |
| --- | --- | --- |
| 入口路線 | 將入口擴成八條路線：初級、中級、高階、Cython、NumPy、Pandas、FastAPI、版本現代化。 | `index.html` |
| 版本狀態 | 版本狀態升為 `v1.0.0`。 | `index.html` |
| 現代化教程 | 新增 Python 3.14 / 3.15 readiness 教程，含 24 張互動卡。 | `python_modernization_engineering_tutorial.html` |
| 進度統計 | 新增 `python-modernization-completed` localStorage key 與 `modernizationProgress`。 | `index.html`、`python_modernization_engineering_tutorial.html` |
| 範例專案 | 新增不依賴第三方套件的版本 readiness sample。 | `範例程式碼/python_version_readiness/` |
| 審查證據 | 新增本輪資深工程審查報告。 | `審查報告/2026-05-13/2026-05-13_020240_Python教程資深工程審查報告.md` |

## 二、內容需要更新的部分已處理

1. 已新增 `python_modernization_engineering_tutorial.html`：
   - release baseline：Python 3.14.5 stable、Python 3.15 beta lane。
   - Python 3.14 features：template string literals、deferred annotations、standard zstd、feature detection。
   - runtime decisions：free-threaded、subinterpreters、GC rollback、asyncio diagnostics。
   - upgrade matrix：runtime matrix、dependency wheel availability、OpenAPI/Pydantic schema regression、C extension binary smoke。
   - delivery governance：feature adoption policy、canary、rollback、release note。
   - sample project：RuntimeProfile、FeaturePolicy、JSON readiness report、unittest。

2. 已新增 `範例程式碼/python_version_readiness/`：
   - `README.md`
   - `pyproject.toml`
   - `src/version_readiness/__init__.py`
   - `src/version_readiness/compat.py`
   - `src/version_readiness/cli.py`
   - `tests/test_compat.py`

3. 已更新 `index.html`：
   - title 與 hero 文案改為八路線。
   - SVG 路線圖新增版本現代化節點。
   - 新增版本現代化教材卡片。
   - 新增 `showProgress("modernizationProgress", "python-modernization-completed", 24)`。
   - 能力地圖新增「可升級」。
   - 版本狀態升為 `v1.0.0`。

## 三、驗證紀錄

| 驗證項目 | 結果 | 證據 |
| --- | --- | --- |
| HTML script parse | 通過 | 10 個 HTML 檔各自的 `<script>` 均可由 Node `new Function()` 解析。 |
| 現代化 lesson count | 通過 | `python_modernization_engineering_tutorial.html`：24 / 24 unique lesson id。 |
| 現代化 lesson group | 通過 | release、features、runtime、upgrade、delivery、sample 各 4 張卡。 |
| 入口 progress key | 通過 | 找到 `python-modernization-completed` 與 `modernizationProgress`。 |
| Python syntax | 通過 | `python3 -m py_compile` 通過 version readiness sample Python 檔。 |
| Sample unittest | 通過 | `PYTHONPATH=範例程式碼/python_version_readiness/src python3 -m unittest discover -s 範例程式碼/python_version_readiness/tests -v`，3 tests passed。 |

## 四、官方版本依據

- Python.org latest release：Python 3.14.5，發布日 2026-05-10。
- Python 3.14 What’s New：Python 3.14 已於 2025-10-07 發布，包含 template string literals、deferred annotations、standard-library subinterpreters、`compression.zstd` 等。
- Python.org 3.15.0b1：Python 3.15 已進入 beta / feature freeze preview，發布日 2026-05-07。

## 五、版本標籤說明

建議 commit message：

```text
Add Python modernization tutorial v1.0.0
```

建議 annotated tag：

```text
v1.0.0
```

Tag 訊息應標註：

- 新增 Python 3.14 / 3.15 readiness 教程。
- 新增版本 readiness sample project。
- 驗證 HTML script parse、24 lesson count、py_compile、unittest。

## 六、Git 執行結果

- 本機 commit：訊息 `Add Python modernization tutorial v1.0.0`；最終 hash 以 `git log --oneline --decorate -1` 為準。
- 本機 annotated tag：`v1.0.0`。
- 推送狀態：執行 `git push origin main --tags` 失敗，原因為目前環境無法解析 `github.com` DNS：`Could not resolve host: github.com`。
- 遠端狀態：本機 `main` 目前 ahead `origin/main` 1 個 commit；`origin/main` 仍停在上一版 `v0.9.0` 對應 commit `f4d50bf`。
