# Python 教程 v1.3.0 更新紀錄

- 更新時間：2026-05-13 05:05:25 CST
- Automation ID：python
- 版本：v1.3.0
- 對應審查報告：`審查報告/2026-05-13/2026-05-13_050525_Python教程資深工程審查報告.md`
- 更新範圍：`index.html`、`python_advanced_engineering_cases.html`

## 一、更新原因

本輪資深工程審查確認教材廣度已足夠，最大缺口在高階 100 案例的語意成熟度與 metadata 對題性。`v1.2.0` 已推進到 30/100 人工語意校閱，但部分語意批次資料與實際案例主題不完全一致；若不修正，會讓讀者看到不相干的 focus、failure path 與 implementation。

## 二、主要修改

| 檔案 | 修改內容 |
|---|---|
| `python_advanced_engineering_cases.html` | 版本標示更新為 `v1.3.0`，人工語意校閱從 30/100 推進到 40/100 |
| `python_advanced_engineering_cases.html` | 修正 `semanticReviewBatchV120` 中不對題的 metadata，使其對應實際 case title |
| `python_advanced_engineering_cases.html` | 新增 `semanticReviewBatchV130`，每章第 4 個案例共 10 個語意深化案例 |
| `python_advanced_engineering_cases.html` | 品質面板新增 `v1.3.0 語意深化` 計數，pending 快速案例改為 60/80 |
| `index.html` | 高階卡片更新為 `100 案例 + 40 語意校閱案例` |
| `index.html` | 版本狀態更新為 `v1.3.0`，說明本輪同時修正 v1.2.0 metadata 並新增第三批語意校閱 |

## 三、v1.2.0 metadata 修正

| Case | 實際案例 | 修正方向 |
|---:|---|---|
| 15 | Plugin Architecture | 改為 extension point、capability registry、plugin sandbox boundary |
| 25 | Read Replica Routing | 改為 consistency window、lag guard、force primary |
| 7 | Threaded File Ingestion | 改為 bounded queue、sentinel shutdown、poison file quarantine |
| 46 | Clock Injection | 改為 Clock Protocol、FakeClock、TTL/排程可測試性 |
| 73 | Snapshot API Response | 改為 schema snapshot、compat allow-list、response version metadata |
| 80 | Per-Tenant Rate Limit | 改為 tenant bucket、quota policy、429 / Retry-After |

## 四、v1.3.0 新增語意深化案例

| 主題 | Case | 更新重點 |
|---|---|---|
| Architecture | Plugin Failure Isolation | timeout、例外隔離、disable switch、核心狀態不變 |
| Data | N+1 Query Detector | query count threshold、bulk loader、list regression gate |
| Concurrency | CPU-bound Report | ProcessPool wrapper、timeout、artifact cleanup |
| Reliability | Circuit Breaker | state machine、half-open probe、cooldown |
| Performance | Cache Stampede | stale-while-revalidate、single-flight refresh、TTL jitter |
| Security | Config Precedence | env/file/defaults merge order、redacted report |
| Testing | Dry Run Mode | plan/apply 分離、confirmation token、rollback artifact |
| Observability | OpenTelemetry Trace | traceparent、background job propagation、PII 過濾 |
| Packaging | Canary Release | rollout percentage、metrics guard、auto rollback |
| Platform | Data Retention | archive manifest、delete audit、retention policy |

## 五、驗證紀錄

本輪已完成：

- `git diff --check`：通過。
- Node HTML script parse：11 個 HTML 檔案全部通過。
- 高階案例數量：`cases=100`、`rendered=100`。
- 語意批次計數：`v120=10`、`v130=10`。
- 人工語意校閱：`reviewed=40`。
- 待深度化案例：`pending=60`。
- `v1.2.0` case number 對題性檢查：通過。
- `v1.3.0` case number 對題性檢查：通過。
- Git commit：`503b565`（`Advance Python case review to v1.3.0`）。
- Git tag：`v1.3.0`。
- Git push：`git push origin main --tags` 成功，`main` 從 `1cff1fd` 推進到 `503b565`，並新增 tag `v1.3.0`。

## 六、版本標註

本輪預計標註版本：

- Git tag：`v1.3.0`
- Commit message：`Advance Python case review to v1.3.0`
- Tag message：`v1.3.0: fix advanced case metadata and add third semantic review batch`
