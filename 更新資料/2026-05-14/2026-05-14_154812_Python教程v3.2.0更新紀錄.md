# Python 教程 v3.2.0 更新紀錄

- 更新日期：2026-05-14
- 完整更新時間：2026-05-14 15:48:12 CST
- 對應審查報告：`審查報告/2026-05-14/2026-05-14_154812_Python教程資深工程審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-05-14/2026-05-14_154812_Python教程v3.2.0內容需要更新的部分.md`
- 更新前版本：v3.1.0
- 更新後版本：v3.2.0

## 1. 本輪更新摘要

本輪依審查報告補上「設定治理工程」獨立路線，讓教程能更完整涵蓋真實 Python 專案中的多環境設定來源、typed validation、secret redaction、feature flag 與 drift troubleshooting。

## 2. 修改內容

| 類型 | 路徑 | 說明 |
|---|---|---|
| 新增教材 | `python_configuration_governance_tutorial.html` | 32 張互動卡，涵蓋 precedence、secret、flag、evidence |
| 新增範例 | `範例程式碼/configuration_governance/` | 標準庫 configuration governance sample |
| 更新入口 | `index.html` | 新增第 22 條教材卡片與進度 key |
| 更新版本 | `VERSION` | 升版為 v3.2.0 |
| 更新紀錄 | `CHANGELOG.md` | 新增 v3.2.0 條目 |

## 3. 驗證結果

本輪已完成以下驗證：

- `git diff --check`：通過
- `python3 -m py_compile 範例程式碼/configuration_governance/src/configuration_governance/*.py 範例程式碼/configuration_governance/scripts/run_configuration_governance_smoke.py`：通過
- `PYTHONPATH=範例程式碼/configuration_governance/src python3 -m unittest discover -s 範例程式碼/configuration_governance/tests -v`：通過，共 6 項測試
- `PYTHONPATH=範例程式碼/configuration_governance/src python3 範例程式碼/configuration_governance/scripts/run_configuration_governance_smoke.py`：通過，輸出 `configuration governance smoke passed: precedence + validation + redaction`
- `node -e "...new Function(script)..."`：通過，輸出 `html script parse passed`

## 4. 風險與注意事項

- 範例以標準庫模擬 secret file / env / CLI precedence，目的是教學與本機驗收，不是完整 secrets manager。
- 正式專案若接 Vault、Kubernetes Secret、AWS Parameter Store 或工業現場密碼管理，仍需依平台補 runtime 存取控制。
- 工業命令與寫入型流程若由設定控制啟用，正式環境必須先審核 fail-safe 行為與回退策略。

## 5. 推送資訊

本輪將以 `v3.2.0` 作為版本標記，並僅提交本次自動化新增或直接更新的檔案；工作樹內其他既有未提交教材修改不納入本次發布。
