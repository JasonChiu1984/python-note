# Python 教程 v3.1.0 更新紀錄

- 更新日期：2026-05-14
- 完整更新時間：2026-05-14 15:07:40 CST
- 對應審查報告：`審查報告/2026-05-14_150740_Python教程資深工程審查報告.md`
- 對應更新清單：`內容需要更新的部分/2026-05-14_150740_Python教程v3.1.0內容需要更新的部分.md`
- 更新前版本：v3.0.0
- 更新後版本：v3.1.0

## 1. 本輪更新摘要

本輪依審查報告補上「事件驅動可靠性」獨立路線，讓教程能更完整涵蓋現代 Python 專案的事件派送、重複處理、故障隔離與修復流程。

## 2. 修改內容

| 類型 | 路徑 | 說明 |
|---|---|---|
| 新增教材 | `python_event_driven_reliability_tutorial.html` | 32 張互動卡，涵蓋 outbox、idempotency、DLQ、replay、ordering、evidence |
| 新增範例 | `範例程式碼/event_reliability/` | 標準庫 event reliability sample |
| 更新入口 | `index.html` | 新增第 21 條教材卡片與進度 key |
| 更新版本 | `VERSION` | 升版為 v3.1.0 |
| 更新紀錄 | `CHANGELOG.md` | 新增 v3.1.0 條目 |

## 3. 驗證結果

本輪已完成以下驗證：

- `git diff --check`：通過
- `python3 -m py_compile 範例程式碼/event_reliability/src/event_reliability/*.py 範例程式碼/event_reliability/scripts/run_event_reliability_smoke.py`：通過
- `PYTHONPATH=範例程式碼/event_reliability/src python3 -m unittest discover -s 範例程式碼/event_reliability/tests -v`：通過，共 6 項測試
- `python3 範例程式碼/event_reliability/scripts/run_event_reliability_smoke.py`：通過，輸出 `event reliability smoke passed: outbox + dedup + dlq + replay + evidence`
- `node -e "...new Function(script)..."`：通過，輸出 `html script parse passed`

## 4. 風險與注意事項

- 事件可靠性範例以標準庫模擬 outbox / broker / consumer，目的是教學與本機驗收，不是完整 broker 實作。
- 正式專案若接 Kafka、RabbitMQ、MQTT 或雲端事件匯流排，仍需依實際平台補 broker-specific delivery semantics。
- 工業命令類事件若涉及設備控制，重送前必須先確認 fail-safe 與現場操作風險。

## 5. 推送資訊

本輪將以 `v3.1.0` 作為版本標記，並僅提交本次自動化新增或直接更新的檔案；工作樹內其他既有未提交教材修改不納入本次發布。
