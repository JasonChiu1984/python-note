# Python 教程 v2.6.0 更新紀錄

- 更新日期：2026-05-13
- 完整更新時間：2026-05-13 18:02:13 CST
- 依據審查報告：`審查報告/2026-05-13_180213_Python教程資深工程審查報告.md`
- 內容清單：`內容需要更新的部分/2026-05-13_180213_Python教程v2.6.0內容需要更新的部分.md`
- 更新版本：v2.6.0

## 1. 本輪更新摘要

本輪新增 `Deployment Runtime` 獨立路線，補齊 Python service 從 release artifact 進入 production runtime 的部署工程缺口。範圍包含 Dockerfile、Compose、環境變數設定、health/readiness、structured logging、SIGTERM graceful shutdown、resource limit、restart policy、image tag/digest、deployment smoke、rollback evidence 與 edge/site handover。

## 2. 新增檔案

| 路徑 | 說明 |
|---|---|
| `python_deployment_runtime_tutorial.html` | v2.6.0 自包含互動式部署執行環境教材，含 28 張 lesson card |
| `範例程式碼/deployment_runtime_service/README.md` | 範例專案使用與驗收說明 |
| `範例程式碼/deployment_runtime_service/pyproject.toml` | 範例 package metadata 與 console entry |
| `範例程式碼/deployment_runtime_service/Dockerfile` | container runtime contract |
| `範例程式碼/deployment_runtime_service/compose.yaml` | Compose service contract |
| `範例程式碼/deployment_runtime_service/.dockerignore` | build context 排除規則 |
| `範例程式碼/deployment_runtime_service/src/deployment_runtime_service/` | 標準庫 runtime service 程式碼 |
| `範例程式碼/deployment_runtime_service/tests/test_runtime_service.py` | unittest 覆蓋 runtime contract |
| `範例程式碼/deployment_runtime_service/scripts/run_deployment_smoke.py` | 本機 smoke 驗收腳本 |

## 3. 修改檔案

| 路徑 | 修改內容 |
|---|---|
| `index.html` | route count 更新為十六條，新增 Deployment Runtime 卡片、SVG 節點、progress key 與 v2.6.0 版本狀態 |
| `VERSION` | 更新為 `v2.6.0` |
| `CHANGELOG.md` | 新增 v2.6.0 更新紀錄 |

## 4. 教材新增能力

- Runtime contract：process、port、config、log、health、shutdown
- Docker：base image、layer、non-root user、`.dockerignore`、healthcheck
- Config：env validation、secret redaction、site config、dependency status
- Health：`/healthz`、`/readyz`、startup gate、draining
- Operations：SIGTERM、resource limit、restart policy、network exposure
- Delivery：image tag、digest、deployment smoke、rollback boundary
- Sample：標準庫 HTTP service、static deployment file validation、unittest、smoke script

## 5. 驗收狀態

完成時間：2026-05-13 18:07:04 CST

已通過：

```bash
git diff --check
node <HTML script parse check>  # html script parse passed: files=19 scripts=19
node <deployment lesson count check>  # lessons=28 unique=28
python3 -m py_compile 範例程式碼/deployment_runtime_service/src/deployment_runtime_service/*.py 範例程式碼/deployment_runtime_service/scripts/run_deployment_smoke.py
PYTHONPATH=範例程式碼/deployment_runtime_service/src python3 -m unittest discover -s 範例程式碼/deployment_runtime_service/tests -v
python3 範例程式碼/deployment_runtime_service/scripts/run_deployment_smoke.py
```

測試結果：

- `unittest`：8 tests passed
- smoke：`deployment runtime smoke passed: config + health + readiness + deployment files`
- Docker build：本輪未執行，因驗收範圍設計為無 Docker daemon 也能先驗證 runtime contract；Dockerfile 與 Compose 已做靜態契約檢查。

## 6. 發布狀態

待 commit、tag 與 GitHub push 完成後補入。
