# Python 教程 v2.1.0 更新紀錄

- 更新時間：2026-05-13 13:01:55 CST
- 來源審查報告：`審查報告/2026-05-13_130155_Python教程資深工程審查報告.md`
- 內容更新清單：`內容需要更新的部分/2026-05-13_130155_Python教程v2.1.0內容需要更新的部分.md`
- 版本：v2.1.0

## 1. Overview

本輪依資深工程審查結果新增「可觀測性與維運工程」作為第十一條 Python 教材路線。v2.0.0 已補上測試工程與品質閘門，因此 v2.1.0 聚焦 production service 上線後的診斷、告警、健康檢查、SLO 與事故回復證據。

## 2. Architecture

| 層級 | 更新內容 |
|---|---|
| 教材入口 | `index.html` 從十條路線更新為十一條路線 |
| 維運教材 | 新增 `python_observability_operations_tutorial.html` |
| 範例程式碼 | 新增 `範例程式碼/observability_operations/` |
| 版本紀錄 | 更新 `VERSION` 與 `CHANGELOG.md` |

## 3. 更新項目

| 項目 | 說明 |
|---|---|
| Logging | JSON structured log、trace id、error context、redaction |
| Metrics | request count、latency、error rate、dependency timeout |
| Tracing | span boundary、downstream propagation、error span、sampling policy |
| Health | liveness、readiness、dependency timeout、degraded status |
| SLO | 使用者導向 SLO、error budget、alert triage、SLO review |
| Incident | timeline、runbook、rollback evidence、postmortem action items |
| Sample | telemetry collector、observed service、health report、SLO evaluation、unittest、smoke script |

## 4. 驗收命令

```bash
git diff --check
node -e 'HTML script parse and observability lesson count check'
python3 -m py_compile 範例程式碼/observability_operations/src/observability_operations/*.py 範例程式碼/observability_operations/tests/*.py 範例程式碼/observability_operations/scripts/run_observability_smoke.py
PYTHONPATH=範例程式碼/observability_operations/src python3 -m unittest discover -s 範例程式碼/observability_operations/tests -v
cd 範例程式碼/observability_operations && python3 scripts/run_observability_smoke.py
```

## 5. 驗收結果

| 檢查 | 結果 |
|---|---|
| `git diff --check` | 通過 |
| HTML script parse | 通過，13 個 HTML 檔案各 1 個 script 可解析 |
| 維運教材卡片數 | 通過，28 張，分組為 logging 4、metrics 4、tracing 4、health 4、slo 4、incident 4、sample 4 |
| `python3 -m py_compile` | 通過，新範例 `src`、`tests`、`scripts` 皆可編譯 |
| `unittest discover` | 通過，4 tests OK |
| smoke script | 通過，`observability smoke passed: logs + metrics + health + SLO` |

## 6. Risk Notes

- 本輪使用標準庫建立零依賴 observability baseline；尚未引入 OpenTelemetry SDK、Prometheus client、Grafana dashboard 或 alert rule。
- 本輪不宣稱已把所有教材範例改成 production telemetry，只新增可觀測性與維運的可執行主線。
