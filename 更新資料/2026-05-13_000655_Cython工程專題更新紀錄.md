# Cython 工程專題更新紀錄

- 更新時間：2026-05-13 00:06:55 CST
- 版本標註：v0.5.0
- 對應審查報告：`審查報告/2026-05-13_000501_Python教程資深工程審查報告.md`
- 更新檔案：
  - `index.html`
  - `python_cython_engineering_tutorial.html`
  - `範例程式碼/cython_so_example/`
  - `.gitignore`
  - `審查報告/2026-05-13_000501_Python教程資深工程審查報告.md`
  - `更新資料/2026-05-13_000655_Cython工程專題更新紀錄.md`

## 一、依審查報告新增的內容

### 1. 入口更新為四路線

`index.html` 從初級 / 中級 / 高階三路線正式擴充為初級 / 中級 / 高階 / Cython 四路線，並更新：

- 頁面 title 與 hero 文案。
- SVG 路線圖，新增 Cython 專題節點。
- 教材卡片，新增 Cython Engineering。
- 能力地圖，新增 Cython hot path、memoryview、wheel、binary release。
- 進度統計，新增 `python-cython-completed` 與 32 個 Cython lesson。

### 2. 新增 Cython 工程實戰教程

新增 `python_cython_engineering_tutorial.html`，定位為獨立效能與 binary delivery 專題，內容包含：

- Cython 簡介、選型流程與工程風險。
- `cythonize -i`、`-a`、`-X`、`-j`、`build_ext`、`python -m build` 指令說明。
- `def` / `cdef` / `cpdef`、typed memoryview、`cdef class`、`.pxd` / `cimport`、`nogil` / `prange`。
- hot loop、C SDK wrapper、package boundary、benchmark、C error handling。
- `pyproject.toml`、wheel、sdist、PyInstaller、CI matrix。
- 6 個專案 slice：影像、金融 tick、log parser、硬體 SDK、資料驗證、CLI release。

### 3. 版本狀態標示

- 入口頁 meta 區塊新增 `v0.5.0 新增 Cython 工程專題`。
- Cython 教程 hero 新增 `v0.5.0`、`32 個互動教學卡`、`指令 / 語法 / 打包 / 專案案例` 狀態標籤。

### 4. 新增最小 `.so` 範例專案

新增 `範例程式碼/cython_so_example/`，提供可讀的 Cython extension 範例檔案：

- `pyproject.toml`
- `setup.py`
- `src/fastmath/_core.pyx`
- `src/fastmath/__init__.py`
- `demo.py`
- `README.md`

並更新 `.gitignore` 忽略 `__pycache__/` 與 `*.pyc`，避免把本機驗證快取納入版本。

## 二、驗證紀錄

- `python_cython_engineering_tutorial.html` script parse：通過。
- Cython lesson id 數量：32。
- Cython unique lesson id 數量：32。
- 主要 HTML script parse：通過。
  - `index.html`
  - `python_advanced_engineering_cases.html`
  - `python_intermediate_engineering_tutorial.html`
  - `python_beginner_interactive_tutorial.html`
  - `python_interactive_tutorial.html`
  - `python_cython_engineering_tutorial.html`
- 入口連結與 key 檢查：找到 `python_cython_engineering_tutorial.html`、`python-cython-completed`、`showProgress("cythonProgress", "python-cython-completed", 32)`。
- Cython 範例程式碼檢查：`setup.py`、`demo.py`、`src/fastmath/__init__.py` 通過 `py_compile`；`pyproject.toml` 含 `setuptools` 與 `Cython>=3` build-system。
- Browser 渲染檢查：受 Browser URL policy 限制，`file://` 本機頁面不可由 Browser 自動化造訪；依安全規則未改用繞路方式開同一個本機頁面。

註：驗證過程中有一次手動命令把檔名誤輸入為 `python_cython_engineing_tutorial.html`，造成預期中的 ENOENT；另一次驗證抓出 lesson 數量應為 32 而非 31。兩者都已修正並用正確命令重跑通過。

## 三、版本說明草稿

版本 `v0.5.0` 標註內容：

- 新增 Cython 工程實戰獨立教程。
- 入口頁擴充為初級 / 中級 / 高階 / Cython 四路線。
- 補上 Cython 指令參數、語法型別、hot path、C SDK wrapper、wheel、PyInstaller 與 CI matrix 教學。
- 補上 Cython lesson 進度追蹤與本輪審查 / 更新紀錄。
