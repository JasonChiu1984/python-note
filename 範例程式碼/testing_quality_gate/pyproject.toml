[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "testing-quality-gate"
version = "0.1.0"
description = "Standard-library Python testing quality gate sample."
requires-python = ">=3.10"
