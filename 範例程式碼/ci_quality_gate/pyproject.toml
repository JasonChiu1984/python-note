[project]
name = "ci-quality-gate"
version = "0.1.0"
description = "Standard-library CI quality gate sample for Python engineering tutorials"
requires-python = ">=3.11"

[tool.ci_quality_gate]
coverage_floor = 80
stable_runtime = "3.14"
beta_runtime = "3.15"
