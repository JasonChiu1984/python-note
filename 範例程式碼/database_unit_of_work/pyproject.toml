[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "database-unit-of-work"
version = "0.1.0"
description = "SQLite repository and unit-of-work sample for Python database engineering lessons."
requires-python = ">=3.10"
