[build-system]
requires = ["setuptools>=68", "wheel", "Cython>=3"]
build-backend = "setuptools.build_meta"

[project]
name = "fastmath"
version = "0.1.0"
description = "Minimal Cython .so extension example"
requires-python = ">=3.10"
