[project]
name = "observability-operations"
version = "0.1.0"
description = "Zero-dependency Python observability and operations teaching sample."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
