[project]
name = "configuration-governance-sample"
version = "0.1.0"
description = "Zero-dependency Python configuration governance sample"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}
