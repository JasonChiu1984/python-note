[project]
name = "event-reliability-sample"
version = "0.1.0"
description = "Zero-dependency Python event-driven reliability sample"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}
