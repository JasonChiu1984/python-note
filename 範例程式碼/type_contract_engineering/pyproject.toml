[project]
name = "type-contract-engineering"
version = "0.1.0"
description = "Standard-library type contract engineering sample for Python tutorials"
requires-python = ">=3.11"

[tool.type_contract_engineering]
schema_version = "1.0"
temperature_range_c = [0, 50]
alarm_threshold_c = 38
stale_timeout_seconds = 30
