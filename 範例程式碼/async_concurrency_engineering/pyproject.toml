[project]
name = "async-concurrency-engineering"
version = "0.1.0"
description = "Standard-library asyncio worker pipeline sample for Python learning notes"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
