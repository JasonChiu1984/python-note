[project]
name = "industrial-data-gateway"
version = "0.1.0"
description = "Standard-library industrial data gateway tutorial sample"
requires-python = ">=3.11"

[tool.gateway]
default_poll_interval_seconds = 5.0
default_timeout_seconds = 2.0
