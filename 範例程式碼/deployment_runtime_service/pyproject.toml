[project]
name = "deployment-runtime-service"
version = "0.1.0"
description = "Zero-dependency Python runtime service sample for deployment engineering lessons."
requires-python = ">=3.11"

[project.scripts]
deployment-runtime-service = "deployment_runtime_service.server:main"

[tool.deployment_runtime]
health_path = "/healthz"
readiness_path = "/readyz"
