# Python 教程 v2.3.0 內容需要更新的部分

- 產生日期：2026-05-13
- 完整產生時間：2026-05-13 15:03:36 CST
- 依據審查報告：`審查報告/2026-05-13_150336_Python教程資深工程審查報告.md`
- 更新版本：v2.3.0

## 1. 更新目標

補上獨立的 `非同步與並發工程` 路線，讓教程從語法、資料、API、資料庫、測試、可觀測性與安全，進一步補齊 Python production 專案常見的 async worker、bounded queue、timeout、cancellation、backpressure 與並發測試能力。

## 2. 預計新增檔案

| 類型 | 路徑 | 說明 |
|---|---|---|
| 教材 | `python_async_concurrency_engineering_tutorial.html` | 自包含互動式 HTML 教材 |
| 範例 | `範例程式碼/async_concurrency_engineering/` | 標準庫 async worker pipeline 範例 |
| 測試 | `範例程式碼/async_concurrency_engineering/tests/` | `unittest.IsolatedAsyncioTestCase` 測試 |
| 驗收 | `範例程式碼/async_concurrency_engineering/scripts/run_async_smoke.py` | smoke script |

## 3. 預計修改檔案

| 路徑 | 修改內容 |
|---|---|
| `index.html` | 新增第十三條路線、Async 卡片、SVG 節點、progress key、版本狀態 |
| `VERSION` | 更新為 `v2.3.0` |
| `CHANGELOG.md` | 新增 v2.3.0 更新紀錄 |
| `更新資料/` | 新增本輪更新紀錄 |

## 4. 教材內容範圍

- event loop 與 coroutine 基礎心智模型
- task、gather、TaskGroup 的選型
- timeout、cancellation、shield 使用邊界
- queue maxsize、producer/consumer、backpressure
- worker lifecycle、drain、graceful shutdown
- retry、jitter、idempotency、error classification
- async testing、failure injection、deterministic checks
- thread/process boundary、CPU-bound 任務處理
- metrics、trace id、release smoke 與 rollback evidence

## 5. 驗收命令

```bash
git diff --check
node <HTML script parse check>
python3 -m py_compile 範例程式碼/async_concurrency_engineering/src/async_concurrency_engineering/*.py 範例程式碼/async_concurrency_engineering/scripts/run_async_smoke.py
PYTHONPATH=範例程式碼/async_concurrency_engineering/src python3 -m unittest discover -s 範例程式碼/async_concurrency_engineering/tests -v
python3 範例程式碼/async_concurrency_engineering/scripts/run_async_smoke.py
```

## 6. 完成定義

- 入口可看到 Async Concurrency Engineering 路線。
- 教材至少有 28 張互動 lesson card。
- 範例專案可在無第三方套件環境執行。
- 測試需覆蓋 success、timeout、retry、backpressure 與 cancellation-safe shutdown。
- `CHANGELOG.md`、`VERSION`、`更新資料` 與 Git tag 需同步到 v2.3.0。
