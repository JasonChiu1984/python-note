# Python 教程 v1.9.0 內容需要更新的部分

- 產出時間：2026-05-13 11:02:04 CST
- 來源審查報告：`審查報告/2026-05-13/2026-05-13_110204_Python教程資深工程審查報告.md`
- 目標版本：v1.9.0

## 1. Overview

本輪不新增新教程路線，專注修正高階 100 案例最後一段語意深度缺口。v1.8.0 已達 90/100，剩餘每章第 10 個案例仍是 template starter 狀態，需補成可作為工程 review 的人工語意校閱案例。

## 2. 更新範圍

| 檔案 | 必要更新 |
|---|---|
| `python_advanced_engineering_cases.html` | 新增 `semanticReviewBatchV190`、納入 `reviewedCaseNumbers()`、更新品質面板 |
| `index.html` | 將高階入口狀態改為 100/100 語意校閱完成 |
| `VERSION` | 更新為 `v1.9.0` |
| `CHANGELOG.md` | 新增 v1.9.0 版本紀錄 |

## 3. 必須補強的 10 個案例

| 案例編號 | 案例名稱 | 需補內容 |
|---:|---|---|
| 82 | Backward Compatible DTO | external contract、schema version、rollback response |
| 43 | Data Dedup Pipeline | stable key、checkpoint、duplicate metric |
| 34 | Priority Queue | priority aging、fairness、queue lag |
| 84 | Reconciliation Job | source of truth、repair command、audit report |
| 97 | Compression Tradeoff | CPU budget、latency、feature flag |
| 53 | Path Traversal 防護 | resolve boundary、tenant scope、audit log |
| 72 | Fuzzing Parser | corpus、timeout、seeded replay |
| 94 | WebSocket Heartbeat | ping/pong、disconnect cleanup、reconnect policy |
| 95 | Streaming Response | chunk iterator、client disconnect、bounded buffer |
| 100 | Vector Search Refresh | versioned index、alias switch、freshness dashboard |

## 4. 驗收條件

- 高階頁顯示 `v1.9.0`。
- 入口頁顯示 `100 案例 + 100 語意校閱案例`。
- 品質面板顯示 `v1.9.0 語意深化 10 / 10`。
- 人工語意校閱顯示 `100 / 100`。
- 快速案例待深度化顯示 `0 / 80`。
- `VERSION` 為 `v1.9.0`。

## 5. 風險備註

- 本輪是內容語意校閱收斂，不應誤解為已替每個案例建立獨立 repo sample。
- 若後續要再提升教材深度，建議從 100 案例中挑 P0/P1 場景建立可執行測試專案，而不是再增加案例數。
