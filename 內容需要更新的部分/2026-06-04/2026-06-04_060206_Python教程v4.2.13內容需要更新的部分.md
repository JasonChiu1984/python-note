# Python 教程 v4.2.13 內容需要更新的部分

- 產出日期：2026-06-04
- 產出時間：2026-06-04 06:02:06 CST
- 依據文件：`審查報告/2026-06-04/2026-06-04_060206_Python教程資深工程審查報告.md`

## 更新目標

本輪不新增新章節，優先修正核心教材頁的版本追溯一致性缺口，並將前一輪尚未推送的 release evidence 一併納入正式發布，讓核心頁、專題頁、首頁、`VERSION`、`CHANGELOG.md` 與遠端發布證據重新對齊。

## 需要更新的項目

| 項目 | 目前狀態 | 需要更新內容 | 原因 |
|---|---|---|---|
| `python_beginner_interactive_tutorial.html` | 顯示 `v4.2.11` | 更新 title、eyebrow 與 footer 版本到 `v4.2.13` | 與當前 repo release 狀態不一致 |
| `python_interactive_tutorial.html` | 顯示 `v4.2.11` | 更新 title、eyebrow 與 footer 版本到 `v4.2.13` | 與當前 repo release 狀態不一致 |
| `python_intermediate_engineering_tutorial.html` | 顯示 `v4.2.11` | 更新 title、eyebrow 與 footer 版本到 `v4.2.13` | 與當前 repo release 狀態不一致 |
| `VERSION` | `v4.2.12` | 升版為 `v4.2.13` | 將本輪維護固化為正式版本 |
| `CHANGELOG.md` | 最新為 `v4.2.12` | 新增 `v4.2.13` 條目 | 保留本輪審查與修正證據 |
| `index.html` | 首頁版本摘要仍以 `v4.2.12` 為最新 | 更新為 `v4.2.13` 並寫明本輪同步三個核心頁與 push evidence 對齊 | 首頁發布摘要需反映最新維護內容 |
| Git 發布證據 | `HEAD` 比 `origin/main` 多一個 commit | 將最終 release commit 與 tag 完整推送 | 遠端需要保留完整發布證據 |

## 不列入本輪的項目

- 不新增新教程章節。
- 不調整首頁路線數量。
- 不修改非版本追溯相關的教學內容與範例程式碼。

## 預期完成定義

1. `rg -n "v4\\.2\\.(11|12|13)"` 應只留下合理歷史敘述，不再出現在本輪目標頁面的過期版本標記位置。
2. `VERSION`、首頁、三個核心頁、三個專題頁與 `CHANGELOG.md` 對齊到 `v4.2.13` / 歷史敘述合理保留。
3. 驗證命令全數通過後，再寫入 `更新資料`。
4. `HEAD`、`origin/main` 與 `v4.2.13^{}` 最終指向同一個 release commit。
