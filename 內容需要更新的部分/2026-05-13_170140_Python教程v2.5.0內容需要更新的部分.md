# Python 教程 v2.5.0 內容需要更新的部分

- 產生日期：2026-05-13
- 完整產生時間：2026-05-13 17:01:40 CST
- 依據審查報告：`審查報告/2026-05-13_170140_Python教程資深工程審查報告.md`
- 更新版本：v2.5.0

## 1. 更新目標

新增獨立的 `Industrial Data Gateway` 路線，讓 Python 教程從一般 software engineering 延伸到此 workspace 的工業自動化主軸：PLC/DDC/SCADA 資料採集、Modbus register mapping、OPC UA NodeId、BACnet object、MQTT topic、timeout、alarm、fail-safe 與現場網路部署。

## 2. 預計新增檔案

| 類型 | 路徑 | 說明 |
|---|---|---|
| 教材 | `python_industrial_data_gateway_tutorial.html` | 自包含互動式 HTML 教材 |
| 範例 | `範例程式碼/industrial_data_gateway/` | 標準庫工業資料採集 gateway 範例 |
| 測試 | `範例程式碼/industrial_data_gateway/tests/` | `unittest` 測試 |
| 驗收 | `範例程式碼/industrial_data_gateway/scripts/run_gateway_smoke.py` | smoke script |

## 3. 預計修改檔案

| 路徑 | 修改內容 |
|---|---|
| `index.html` | 新增第十五條路線、Industrial 卡片、SVG 節點、progress key、版本狀態 |
| `VERSION` | 更新為 `v2.5.0` |
| `CHANGELOG.md` | 新增 v2.5.0 更新紀錄 |
| `更新資料/` | 新增本輪更新紀錄 |

## 4. 教材內容範圍

- Gateway architecture、signal flow、IO mapping
- Modbus TCP/RTU：FC03、FC16、40001 Holding Register、word order、float conversion
- OPC UA：`opc.tcp://192.168.10.20:4840`、`ns=2;s=Boiler.Temperature`、Security=None、subscription、publish、keepalive、reconnect
- BACnet/IP：analog-input object instance、BBMD、foreign device、routing
- MQTT：topic naming、JSON payload、QoS、store-and-forward
- polling interval、timeout、retry、stale data、alarm handling、fail-safe output
- VLAN、industrial firewall、VPN routing、SCADA isolation 與維護診斷

## 5. 驗收命令

```bash
git diff --check
node <HTML script parse check>
python3 -m py_compile 範例程式碼/industrial_data_gateway/src/industrial_data_gateway/*.py 範例程式碼/industrial_data_gateway/scripts/run_gateway_smoke.py
PYTHONPATH=範例程式碼/industrial_data_gateway/src python3 -m unittest discover -s 範例程式碼/industrial_data_gateway/tests -v
python3 範例程式碼/industrial_data_gateway/scripts/run_gateway_smoke.py
```

## 6. 完成定義

- 入口可看到 Industrial Data Gateway 路線。
- 教材至少有 32 張互動 lesson card。
- 範例專案可在無第三方套件環境執行。
- 測試需覆蓋 Modbus float conversion、alarm、timeout、fail-safe 與 gateway report。
- `CHANGELOG.md`、`VERSION`、`更新資料` 與 Git tag 需同步到 v2.5.0。
