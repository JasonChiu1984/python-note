# Python 教程 v4.2.21 內容需要更新的部分

- 產出日期：2026-06-12
- 產出時間：2026-06-12 06:01:25 CST
- 依據文件：`審查報告/2026-06-12/2026-06-12_060125_Python教程資深工程審查報告.md`

## 更新目標

本輪不新增新章節，也不修改既有教材知識面。更新目標是修正 `v4.2.20` maintenance release 遺留的 page-level visible version drift：同步五個帶版本標記的主教材/專題頁、`VERSION`、`CHANGELOG.md`、首頁版本狀態與 dated artifacts 到 `v4.2.21`，讓 repo metadata、首頁描述與實際教材頁 release state 回到同一個可追溯來源。

## 需要更新的項目

| 項目 | 目前狀態 | 需要更新內容 | 原因 |
|---|---|---|---|
| `審查報告/2026-06-12/...` | 尚未建立 | 新增本輪審查報告 | 保留今日複核依據 |
| `內容需要更新的部分/2026-06-12/...` | 尚未建立 | 新增本輪更新清單 | 保留 staged workflow 中間產物 |
| `更新資料/2026-06-12/...` | 尚未建立 | 驗證完成後寫入本輪更新紀錄 | 保留最終發布紀錄 |
| `python_beginner_interactive_tutorial.html` | visible version 仍為 `v4.2.19` | 同步頁面標題、eyebrow、footer release wording 到 `v4.2.21` | 修正核心教材頁 drift |
| `python_intermediate_engineering_tutorial.html` | visible version 仍為 `v4.2.19` | 同步頁面標題、eyebrow、footer release wording 到 `v4.2.21` | 修正核心教材頁 drift |
| `python_cli_automation_engineering_tutorial.html` | visible version 仍為 `v4.2.19` | 同步標題與 status strip 到 `v4.2.21` | 修正專題頁 drift |
| `python_local_knowledge_base_mcp_tutorial.html` | visible version 仍為 `v4.2.19` | 同步標題與 status strip 到 `v4.2.21` | 修正專題頁 drift |
| `python_project_delivery_blueprint_tutorial.html` | visible version 仍為 `v4.2.19` | 同步標題、eyebrow、footer release wording 到 `v4.2.21` | 修正專題頁 drift |
| `python_interactive_tutorial.html` | `v4.2.18` legacy alias maintenance | 維持不變 | 這是刻意保留的 canonical-route alias page，不屬於 drift |
| `VERSION` | `v4.2.20` | 升版為 `v4.2.21` | 以新 patch release 收斂本輪 drift |
| `CHANGELOG.md` | 最新為 `v4.2.20` | 新增 `v4.2.21` page-level traceability maintenance 條目 | 保留本輪 release evidence |
| `index.html` | 首頁版本摘要誤稱六個版本標記頁都與 `v4.2.20` 對齊 | 更新為 `v4.2.21` 並改寫為本輪 drift 修正敘述 | 首頁需反映真實最新 release state |
| Git 發布證據 | 尚未建立 | 完成 commit、annotated tag 與 push | 補齊遠端發布鏈 |

## 不列入本輪的項目

- 不新增新教程章節。
- 不調整 `python_interactive_tutorial.html` 的 legacy alias 設計。
- 不修改首頁教材結構、卡片數量或路線分類。
- 不變更 knowledge base tooling、測試契約與範例程式碼內容。

## 預期完成定義

1. 今日三份 dated artifacts 建立完成，且互相交叉引用正確。
2. 五個 drift 頁面的 visible version labels 與 repo release state 對齊到 `v4.2.21`。
3. `VERSION`、`CHANGELOG.md` 與首頁發布狀態對齊到 `v4.2.21`。
4. 既有驗證命令全數通過後，再寫入本輪 `更新資料`。
5. `HEAD`、`origin/main` 與 `v4.2.21^{commit}` 最終指向同一個 release commit；若網路失敗，需明確標示為本地完成但遠端未重新對齊。
