# Python 教程 v4.2.8 內容需要更新的部分

- 建立時間：2026-05-27 06:10:24 CST
- 來源審查報告：`審查報告/2026-05-27/2026-05-27_061024_Python教程資深工程審查報告.md`
- 版本目標：`v4.2.8`

## 一、更新主軸

本次不是新增新章節，而是修正三個專題頁的版本追溯漂移，讓 CLI 自動化工程、本地知識庫 MCP 與 Project Delivery Blueprint 三條專題路線重新與首頁、`VERSION`、`CHANGELOG.md` 對齊。

## 二、需要更新項目

| 項目 | 狀態 | 說明 |
| --- | --- | --- |
| `VERSION` | 本次修正 | 由 `v4.2.7` 推進為 `v4.2.8`。 |
| `CHANGELOG.md` | 本次修正 | 新增 `v4.2.8` 條目，記錄三個專題頁版本追溯一致性修正。 |
| `index.html` 版本狀態文案 | 本次修正 | 說明本輪聚焦於三個專題頁的版本同步。 |
| `python_cli_automation_engineering_tutorial.html` | 本次修正 | 將 title 與 hero status 版本由 `v4.2.6` 同步為 `v4.2.8`。 |
| `python_local_knowledge_base_mcp_tutorial.html` | 本次修正 | 將 title 與 hero status 版本由 `v4.2.6` 同步為 `v4.2.8`。 |
| `python_project_delivery_blueprint_tutorial.html` | 本次修正 | 將 title、hero 標記、footer 版本由 `v4.2.6` 同步為 `v4.2.8`。 |
| 本輪交付文件 | 本次新增 | 補齊 `v4.2.8` 的審查報告、修正清單與更新紀錄。 |

## 三、實作原則

1. 只修正發布追溯資訊，不改教學內容結構、範例程式行為與學習路線數量。
2. 保持既有課程卡數、互動內容與驗證命令不擴張，避免維護版失焦。
3. 首頁、核心教材頁、專題頁、`VERSION`、`CHANGELOG.md` 應維持單一事實來源。
4. `PythonReleaseNote/` 仍排除在本次發布之外。

## 四、完成定義

- `VERSION` 更新為 `v4.2.8`
- `CHANGELOG.md` 包含 `v4.2.8`
- 首頁版本狀態更新為 `v4.2.8`
- 三個專題頁不再殘留 `v4.2.6` 舊版標記
- 本輪交付文件已存放於 `2026-05-27/` 子目錄
- `main`、`origin/main` 與 `v4.2.8^{}` 最終對齊到同一提交
