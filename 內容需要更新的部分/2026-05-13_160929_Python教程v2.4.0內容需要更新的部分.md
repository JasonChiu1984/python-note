# Python 教程 v2.4.0 內容需要更新的部分

- 產生日期：2026-05-13
- 完整產生時間：2026-05-13 16:09:29 CST
- 依據審查報告：`審查報告/2026-05-13_160929_Python教程資深工程審查報告.md`
- 更新版本：v2.4.0

## 1. 更新目標

補上獨立的 `Packaging / Release Engineering` 路線，讓教程從語法、資料、API、資料庫、測試、可觀測性、安全與 async worker，進一步補齊 Python production 專案最常見的 package metadata、版本契約、artifact、checksum、manifest、release gate 與 rollback evidence。

## 2. 預計新增檔案

| 類型 | 路徑 | 說明 |
|---|---|---|
| 教材 | `python_packaging_release_engineering_tutorial.html` | 自包含互動式 HTML 教材 |
| 範例 | `範例程式碼/packaging_release_engineering/` | 標準庫 release gate 範例 |
| 測試 | `範例程式碼/packaging_release_engineering/tests/` | `unittest` 測試 |
| 驗收 | `範例程式碼/packaging_release_engineering/scripts/run_release_smoke.py` | smoke script |

## 3. 預計修改檔案

| 路徑 | 修改內容 |
|---|---|
| `index.html` | 新增第十四條路線、Packaging 卡片、SVG 節點、progress key、版本狀態 |
| `VERSION` | 更新為 `v2.4.0` |
| `CHANGELOG.md` | 新增 v2.4.0 更新紀錄 |
| `更新資料/` | 新增本輪更新紀錄 |

## 4. 教材內容範圍

- `pyproject.toml` metadata contract
- src layout、package import boundary、console script entry point
- semantic version、tag policy、release branch policy
- changelog gate、release note、artifact evidence
- source archive、wheel、executable 的用途與風險
- deterministic archive、SHA-256 checksum、manifest
- dependency manifest、SBOM、license 與 provenance
- CI quality gate、smoke、rollback、promotion
- 外部發布權限與本地可驗證項目的分界

## 5. 驗收命令

```bash
git diff --check
node <HTML script parse check>
python3 -m py_compile 範例程式碼/packaging_release_engineering/src/packaging_release_engineering/*.py 範例程式碼/packaging_release_engineering/scripts/run_release_smoke.py
PYTHONPATH=範例程式碼/packaging_release_engineering/src python3 -m unittest discover -s 範例程式碼/packaging_release_engineering/tests -v
python3 範例程式碼/packaging_release_engineering/scripts/run_release_smoke.py
```

## 6. 完成定義

- 入口可看到 Packaging / Release Engineering 路線。
- 教材至少有 28 張互動 lesson card。
- 範例專案可在無第三方套件環境執行。
- 測試需覆蓋版本一致性、changelog gate、archive、checksum 與 manifest。
- `CHANGELOG.md`、`VERSION`、`更新資料` 與 Git tag 需同步到 v2.4.0。
